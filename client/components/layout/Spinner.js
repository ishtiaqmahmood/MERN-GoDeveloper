import React from "react";

const Spinner = () => (
  <React.Fragment>
    <img
      src="/img/spinner.gif"
      className="w-[200px] m-auto block"
      alt="Loading..."
    />
  </React.Fragment>
);

export default Spinner;
