import React from "react";

const Spinner = () => (
  <React.Fragment>
    <img
      src="/img/spinner.gif"
      style={{ width: "200px", margin: "auto", display: "block" }}
      alt="Loading..."
    />
  </React.Fragment>
);

export default Spinner;
